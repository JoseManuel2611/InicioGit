const resultado = 3 + 3 * 3 - 5 + 3;

// ...existing code...

function definirColor(color: string): string {
    if (color.toLowerCase() === "rojo") {
        return "Color correcto";
    } else {
        return "Color incorrecto";
    }
}

// Ejemplo de uso:
console.log(definirColor("rojo"));      // Color correcto
console.log(definirColor("azul"));      // Color incorrecto
// ...existing code...

